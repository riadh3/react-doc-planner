import React from 'react';

const ImproveLives = (props) =>
    <div className="improve-lives">
       <h1 className="improve-lives-title">{props.title}</h1>
       <p className="description">{props.description}</p>
    </div>

export default ImproveLives 
